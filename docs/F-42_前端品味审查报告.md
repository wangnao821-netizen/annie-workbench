# F-42 前端品味审查报告（2026-08-15）

> 审查对象：`ui/vera-工作台 (61)`（F-41c 交付后最新版）
> 审查方法：三套设计 skill 交叉审查——taste-skill `redesign-existing-projects`（设计审计）
> ＋ web-design-engineer `garden-skills`（五维批判）＋ 内置 apple-design（动效专项）
> 结论：**6.2 / 10 —— 令牌地基已打好，组件层执行只完成 1/3；动效动作齐全但语言碎片化**

---

## 一、五维评分（web-design-engineer 框架）

| 维度 | 分 | 关键证据 |
|---|---|---|
| 方向一致性 | 5 | 令牌层单 accent 已收敛，组件层紫色失控、5 种 Tailwind 色叛逃 |
| 视觉层级 | 6 | 布局层次清楚，但全站字号扁平在 12px 附近，标题对比弱 |
| 工艺质量 | 6 | 圆角令牌 0 消费、语义色消费不足 1/3、黑白透明度硬编码遍地 |
| 功能性 | 8 | 交互状态、空态、加载态、焦点环、reduced-motion 骨架全覆盖 |
| 原创性 | 6 | 已脱离大块 AI 模板味，但渐变头像/贴士、emoji 混排仍带旧味道 |

---

## 二、问题清单（按修复优先级）

### P0 · 色彩收口（F-41c 未竟之业，量最大）

1. **紫色失控，语义漂移**。全站 509 处 `purple-XXX` 硬编码 + 52 处 `var(--purple)` + 2 处 `#8b5cf6`。
   F-41 定稿"紫色仅保留为智能 AI 特殊高亮辅助"，实际它同时代表 4 种含义：
   AI 高亮（BrainChat 工具、计算器）、澳洲时区（AuTimePanel 整面板）、老板决策（筛选 tab/徽章）、
   关联案件（DetailPanel 横幅）。一个颜色 4 种含义 = 没有含义。
   **处置：紫色只留给 AI 相关；时区/老板/关联案件分别换 accent / yellow / blue 语义。**
2. **Tailwind 默认色叛逃**。`rose/amber/emerald/indigo/blue/gray` 直接使用遍布 80 个文件
   （HomePage 22 处、BrainChat 21 处、CasePanorama 15 处）。切到 ivory/护眼绿/柔光粉主题时
   这些颜色纹丝不动，主题体系形同虚设。全部应收敛到 `var(--red/--yellow/--green/--orange/--accent)` + soft 变体。
3. **黑白透明度硬编码**。`bg-black/5 dark:bg-white/10` 这类遍地（预估 300+ 处），
   在 sand/ivory 主题下显脏。应收成 `--bg-subtle` 一类令牌。
4. **AI 渐变残留**：Sidebar logo（accent→purple）、首页专家贴士（indigo→purple→amber 三色）、
   头像（amber→rose→purple）、CasePanorama 空态（purple→pink）。taste-skill 点名的"AI 指纹"。

### P0 · 动效统一（apple-design 专项，改动小感知强）

1. **Spring 语言碎片化**：16 种 spring 参数组合（damping 20/24/25/26/28/30/35 × stiffness 300-500）。
   Apple 标准：默认阻尼 1.0（无过冲），只有甩动/投掷才允许弹跳。全站浮层普遍带轻微 overshoot。
   **处置：定 2 档 house style——浮层/下拉/弹窗 `damping:25, stiffness:300`；
   卡片 hover/抽屉拖拽才允许 `stiffness:350-400, damping:20-25`；其余 14 种全部收编。**
2. **Reduced-motion 覆盖率 42%**：534 个 motion 组件，仅 76/180 文件有 `useReducedMotion`。
   关键坑：index.css 的全局 `prefers-reduced-motion` 拦不住 Motion 库的 JS spring 动画，
   必须组件级补分支。重灾区：TaskCard(12)、GlobalExperienceTab(10)、CaseFolderCard(8)、
   OverdueDetail(6)、FloatingAI(6)。
3. **同族元素手感不一致**：弹窗进入位移 `y:6/8/10/12` 四种；卡片 hover `y:-1/-2/-3` 三种。
4. **弹层未锚定触发源**：通知/头像/时区下拉从中心展开，未从触发按钮 origin 生长。

### P1 · 字号体系与可读性

1. 全站 ≤12px 字号共 1459 处：12px×659、11px×353、**10px×404、9px×39**。9-10px 中文基本不可读。
2. 796 处固定 px 字号（`text-[10px]` 等）不随系统字号缩放（违反 rem 原则）。
3. 建议：建立字号令牌（12/13/14/16 为主梯队），9-10px 消灭，11px 只留给时间戳，
   关键操作和金额 ≥12px，区块标题 ≥14px。

### P2 · 表面层级与结构

1. 玻璃拟态 56 处，静态容器（侧栏/面板/header）也在用玻璃——只应留给浮层。
2. 三套导航并存：TopNavBar 头像下拉（知识/档案/草稿/设置）＋ CaseListSidebar 底部 tab（首页/看板/统计/设置）——重复。
3. `Sidebar.tsx` 已不被 AppShell 引用（死代码，应删）。
4. 主题切换 4 入口：顶栏 SunMoon 循环 + Sidebar Palette + Sidebar logo 点击 + 设置页 ThemePicker。

### P3 · 细节

1. emoji 当 UI 元素混排（快捷提问 🧮📂、徽章 👑📧、状态 ✅⛔）。渠道徽章建议换 lucide 图标。
2. 圆角令牌 `--radius` 定义 0 消费（全站 rounded-xl 497 处等，统一性尚可但改不了策略）。
3. **硬编码 mock 数据泄漏到 UI**：首页贴士写死"陈伟案件 (NAB)"、通知里写死客户名——联调前清理，PII 风险。
4. 拖拽分隔条 PanelDivider 用 `bg-purple-500/50`——紫色硬编码。
5. 过渡动画 spring 参数各组件不一致（详见 P0 动效）。

---

## 三、做得好的（审查也要认账）

- NotificationBell / CalculatorPanel / FolderPickerModal（F-41c 清理过的活跃组件）确实干净，是推广样板。
- 全局 focus-visible ring、prefers-reduced-motion/reduced-transparency/contrast 三连覆盖、select 原生弹层 6 主题覆盖。
- 空态/加载态/错误态覆盖好；按钮 whileTap 即时反馈普遍。
- 无 CSS keyframes、无全屏背景动画；拖拽分隔条 Pointer 事件 + 捕获 + 1:1 跟手（apple-design 标准答案）。
- 右栏 CasePanorama 结构完全符合规划（关键截止≤3、下一步≤5、风险情报、折叠快照），只是颜色没跟上。

---

## 四、修复批次规划（F-42 分批）

| 批次 | 内容 | 预计 AI Studio 轮次 |
|---|---|---|
| F-42a | P0 色彩收口 + P0 动效 house style（可同一轮） | 1 |
| F-42b | P1 字号令牌体系 + reduced-motion 补全覆盖 | 1 |
| F-42c | P2 玻璃只留浮层 + 导航精简（死代码/入口）+ 弹层锚定 | 1-2 |
| F-42d | P3 细节：emoji 收敛、圆角令牌、mock 清理、PanelDivider 色 | 1 |

> 每批验收后开下一批；批次内改动必须附完整替换清单（文件/行/原类名/新类名）。

---

## 五、F-42a 验收记录（2026-08-15，对照 ui/vera-工作台 (62)）

### 结论：❌ 验收不通过（色彩约完成 40%，动效 0%）

**已完成（点名重点组件 + 令牌基建）：**

- tokens.css 六主题新增 `--bg-subtle / --bg-subtle-strong / --mark-act/nsw/qld/cn` ✓
- TopNavBar：时钟/铃铛/主题图标紫色 → text-secondary、通知徽章 → var(--red)、
  通知类型色 → var(--red/yellow/green)、头像三色渐变 → var(--accent)、搜索聚焦 → var(--ring) ✓
- Sidebar logo 渐变 → var(--accent) 单色；DetailPanel 关联案件 → accent-soft；
  PanelDivider → var(--accent)；CaseListSidebar 老板 tab → var(--yellow)；
  AuTimePanel 紫色清零并启用 --bg-subtle；HomePage 今日待办/贴士/快捷发问/统计卡 var() 化 ✓
- `--bg-subtle` 已在 7 个文件启用 24 处 ✓

**未完成（全站机械替换缺失 + 动效未做）：**

1. **tailwind 色类残留 632 处**：purple- 237、amber- 128、emerald- 94、rose- 87、blue- 73、
   red- 16、gray- 9、indigo- 2、pink- 2、green- 1。重灾区：FlowDialogCard（purple 20+）、
   TaskDrawer（rose/amber/blue/purple/gray 30+）、PolicyHintCard（emerald/rose/amber/purple 20+）、
   Analytics、DraftsBox、ImportHistory、Archive、Migration、OsWorkbench/OsConditionsColumn/
   OsDraftColumn/OsStrategyColumn、CaseBoard、CaseCard、TaskCard、TaskDetailOverlay、
   GlobalStatsPanel、RecordedEventsDrawer、RiskSection、SubmissionBanner、ManualNoteModal、
   ChatPanel、KnowledgeCenter、Settings、CaseDetail、KpiBar、ErrorBoundary、FloatingAI、
   FloatingAIMessages、ChecklistDrawerContent、DelegateDialog、CasePanorama（空态渐变未改）。
2. **bg-black / dark:bg-white 残留 180 处**（47 个文件）：TopNavBar ⌘K 徽章、搜索结果 lender 徽章、
   Analytics 骨架/表格 hover、Archive/ImportHistory 骨架等。
3. **动效完全未做**：spring 参数 40+ 种组合原样（damping 20-35 × stiffness 300-500），
   无任何收编；初始位移 y:6/8/10/12 未统一；hover lift y:-1/-2/-3 未统一。
4. **渐变残留**：FloatingAI `linear-gradient(135deg, var(--accent), #8b5cf6)`（硬编码紫）、
   FloatingAIMessages `from-blue-500 to-indigo-600 / from-purple-500 to-pink-500`、
   AssistantOnboardingCard / SkillCenter 渐变卡、CasePanorama 空态 purple→pink。

### 处置

追加 **F-42a 补丁**（见前端改造批次提示词文档），要求逐文件机械替换 + 动效两档收编，
交付前必须跑 rg 自查达到数字级验收标准（632→0、180→0、spring 仅两档）。

---

## 六、F-42a 补丁验收记录（2026-08-15，对照 ui/vera-工作台 (63)）

### 结论：✅ 主体通过（硬指标全绿），4 处小问题出补丁二

**硬性自查（全部达标）：**

| 项 | 结果 |
|---|---|
| tailwind 色类（purple/rose/amber/emerald/blue/gray 等 -数字） | 632 → **0** ✓ |
| bg-black / dark:bg-white | 180 → **0** ✓ |
| spring 参数 | 40+ 种 → **2 种**（300/400 两档）✓ |
| hex 残留 | 仅白字/ThemePicker 预览色 ✓ |
| --bg-subtle / --mark-* 令牌 | 六主题齐全 ✓ |
| 渐变清理 | 多色硬编码渐变 → 令牌双色渐变 ✓ |

**需要补丁二修的 4 类小问题：**

1. **坏渐变 2 处（真实渲染 bug）**：`FloatingAIMessages.tsx:64` 与 `CasePanorama.tsx:208`
   的 `bg-gradient-to-r var(--accent)` / `bg-gradient-to-tr var(--accent-soft)` ——
   `bg-gradient-to-*` 后直接跟 var() 不是有效 Tailwind 渐变，应改 `bg-[var(--accent)]` 单色底。
2. **--amber 未定义令牌 2 处**：`KanbanCard.tsx:104`、`OsDraftColumn.tsx:48`
   用了 `var(--amber, #f59e0b)` / `var(--amber-soft, ...)`——tokens.css 无 --amber（只有 --yellow），
   应改 `var(--yellow)` / `var(--yellow-soft)`（否则 sand 等主题下与主题黄色有色差）。
3. **spring 无参数 1 处**：`BrainChat.tsx:832` `type: 'spring',` 无参数（framer 默认 100/10，
   与两档不一致），应补 `damping: 25, stiffness: 300`。
4. **紫色语义残留 3 处**：`TaskCard.tsx:22` BOSS_DECISION 徽章紫色（老板语义应为 yellow，
   与 TaskDrawer 不一致）、`OverviewFacts.tsx:43/44/124` 银行/交易结构紫色（应为 accent）、
   `CaseFolderCard.tsx:182` `_Inbox` 目录名紫色（系统约定高亮，可改 accent）。

---

## 七、F-42a 补丁二验收记录（2026-08-15，对照 ui/vera-工作台 (64)）

### 结论：✅ F-42a 全链路验收通过（F-42a + 补丁一 + 补丁二全绿）

**8 处修改逐条验证全部到位：**

| # | 位置 | 验证结果 |
|---|---|---|
| 1 | FloatingAIMessages.tsx:64 | `bg-[var(--accent)]` / `bg-[var(--purple)]` ✓ |
| 2 | CasePanorama.tsx:208 | `bg-[var(--accent-soft)]` ✓ |
| 3 | KanbanCard.tsx:104 | `var(--yellow)` ✓ |
| 4 | OsDraftColumn.tsx:48 | `var(--yellow-soft)` / `var(--yellow)` ✓ |
| 5 | BrainChat.tsx:832 | `damping:25, stiffness:300` ✓ |
| 6 | TaskCard.tsx:22 | BOSS_DECISION → `var(--yellow-soft)`/`var(--yellow)` ✓ |
| 7 | OverviewFacts.tsx:43/44/124 | `var(--accent)` ✓ |
| 8 | CaseFolderCard.tsx:182/367 | `_Inbox` → `var(--accent)` ✓ |

**全站复查（rg 精确统计）：**

| 项 | 结果 |
|---|---|
| tailwind 色类（-数字） | **0** ✓ |
| bg-black / dark:bg-white | **0** ✓ |
| spring 参数 | 仅 2 档：`damping:25, stiffness:300` ×44、`400` ×14 ✓ |
| 裸 `type: 'spring',` | **0** ✓ |
| `var(--amber`（未定义令牌） | **0** ✓ |
| `bg-gradient-to-* var(`（坏渐变） | **0** ✓ |

**F-42a 批次（色彩收口 + 动效统一）正式收口。**

---

## 八、F-42b 数据基线（2026-08-15，对照 ui/vera-工作台 (64)）

**字号分布：**

| 字号 | 数量 | 处置 |
|---|---|---|
| text-[8px] | 4 | 消灭 → 10px |
| text-[9px] | 39 | 消灭 → 10px（或 11px） |
| text-[10px] | 404 | 关键信息 → 12px；次要 → 11px |
| text-[11px] | 353 | 保留（时间戳/辅助） |
| text-xs (12px) | 657 | 基准正文 |
| text-sm (14px) | 66 | 次级标题 |
| text-base (16px) | 15 | 正文 |

**reduced-motion 覆盖：** 107 文件、83 个含 motion 组件、仅 38 个有 useReducedMotion，
**45 个文件需补**（TOP：TaskCard 12、GlobalExperienceTab 10、CaseFolderCard 8、
OverdueDetail/FloatingAI/FileMatchDetail/NewClientDetail 6、CoCreateDialog 5、
DraftEditor/BossDecisionDetail/Migration/OsDraftColumn/OsAttackDetail/Archive/
SettlementDetail/DraftsBox/ChecklistPanel/ImportHistory/KpiBar/AssistantOnboardingCard 4 等）。

---

## 九、F-42b 验收记录（2026-08-15，对照 ui/vera-工作台 (65)）

### 结论：✅ F-42b 验收通过（字号体系 + reduced-motion 全绿）

**字号（rg -F 精确统计）：**

| 字号 | 基线 (64) | (65) | 结论 |
|---|---|---|---|
| text-[8px] | 4 | **0** | ✓ 消灭 |
| text-[9px] | 39 | **0** | ✓ 消灭 |
| text-[10px] | 404 | **0** | ✓ 全消灭（超预期） |
| text-[11px] | 353 | 585 | ✓ 次要信息落位 |
| text-xs (12px) | 657 | 872 | ✓ 关键信息升级（+215） |
| text-sm/base | 66/15 | 66/15 | 不变 ✓ |

抽查：HomePage 按钮/徽章均 text-xs font-bold；CasePanorama 客户名/阶段徽章 text-xs；
8-10px 全部消灭。数字勾稽：8/9/10px 共 447 处 = 11px 增 232 + 12px 增 215 ✓。

**reduced-motion：** 有 motion 组件文件 83 个，useReducedMotion 覆盖 **83/83 = 100%** ✓
（基线 38，补 45）；样板抽查 TaskCard/GlobalExperienceTab 写法规范
（`whileTap={reduced ? undefined : ...}`、reduced 下仅 opacity）✓。

**回归：** tailwind 色类 0、bg-black 0、spring 仅 300/400 两档、裸 spring 0 —— F-42a 成果零破损 ✓。

**F-42b 批次（字号 + 无障碍）正式收口。**

---

## 十、F-42c 数据基线（2026-08-15，对照 ui/vera-工作台 (65)）

1. **玻璃拟态 22 个文件**：浮层（弹窗/抽屉/下拉）合理保留——CalculatorPanel、CoCreateDialog、
   ChecklistDrawer、FileDrawer、TaskDrawer、FlowDialogCard、RecordedEventsDrawer、
   ContextPreviewModal、FactAmendModal、ManualNoteModal、TaskDetailOverlay；
   静态 chrome 需改实底——TopNavBar（header）、CaseListSidebar（左栏）、CasePanorama（右栏）、
   HomePage（页面卡片）、BrainChat（输入区）、GlobalStatsPanel、SkillCenter、
   CalculatorManager、BankPlatformPanel、OverviewTimeline、FactCard。
2. **死代码**：`Sidebar.tsx` 无任何引用（AppShell 用 CaseListSidebar），应删除。
3. **弹层锚定**：transformOrigin 仅 4 处（KpiBar top right、Sidebar top left[死代码]、
   NotificationBell top left、FilePreviewPanel center）；TopNavBar 的 AU 时间/通知/头像三个
   下拉均未锚定触发按钮（从中心展开）。
4. **主题切换入口**：实际仅 2 个（顶栏 SunMoon 循环 + 设置页 ThemePicker；Sidebar 死代码不计）——可接受，不再精简。

---

## 十一、F-42c 验收记录（2026-08-15，对照 ui/vera-工作台 (66)）

### 结论：✅ F-42c 验收通过（玻璃分层 + 死代码 + 弹层锚定全绿）

1. **Sidebar.tsx 已删除** ✓（Test-Path False，AppShell 无失效引用）
2. **玻璃只留浮层** ✓：16 个文件 28 处 glass 全部核实为浮层——搜索下拉（TopNavBar:243）、
   工具菜单（BrainChat:1231）、递交确认弹窗（BrainChat:1436）、SkillCenter 4 个弹窗、
   OverviewTimeline 小弹窗、FactCard 气泡、CalculatorPanel/CoCreateDialog/各 Drawer 等；
   静态 chrome（顶栏/左右栏/首页卡片/页面容器）已全部实底化。
3. **弹层锚定** ✓：TopNavBar AU 时间/通知/头像三个下拉 top right、BrainChat 工具菜单
   bottom left、CaseMemoryTab top right、FactCard bottom right、NotificationBell top left。
4. **回归零破损** ✓：色类 0、bg-black 0、spring 两档 55 处、裸 spring 0、字号 8-10px 0、
   reduced-motion 82/82（含 motion 文件数因删 Sidebar 从 83→82）。

**F-42c 批次（结构层）正式收口。**

---

## 十二、F-42d 数据基线（2026-08-15，对照 ui/vera-工作台 (66)）

**无门控硬编码 mock PII（红线相关，必须清）：**

1. `TopNavBar.tsx:31-33` INITIAL_NOTIFICATIONS：写死"陈伟 (NAB Bank) 补件超期预警 /
   PERSON_1 (CBA) / 西太银行预审通过"——真实人名风格 + 银行 + 金额上下文，无 mock 门控，必清；
2. `HomePage.tsx:589/595` 专家贴士：写死"陈伟案件 (NAB)…PERSON_1 (CBA)…"——无门控，必清；
3. `BrainChat.tsx:307`：`caseId === 'CASE_001' || caseId === 'CASE-2026-0801'` 硬编码案件特例
   （mock 逻辑），联调会误匹配，应删；
4. `FolderLookupCard.tsx:28-29`：mock 摘要写死"雇主 Tech Corp / $180,000"——在
   VITE_USE_MOCK 门控内，标注"演示数据"即可，不强清。

**有门控（联调后消失，不动）：** Archive/DraftsBox/BrainChat 的 PERSON_1 数据均在
VITE_USE_MOCK 分支内。

**emoji 收敛（建议）：** 任务来源徽章 👑📧📁🏦⚙️（TaskDrawer/CasePanorama）、快捷提问 🧮📂、
状态 ✅⛔🏖️ 等——渠道徽章换 lucide 图标，装饰性 emoji 删除，仅保留 🔒（不能给银行看）等表意清晰的。

**圆角令牌（--radius 0 消费）：** 记入 BACKLOG 技术债，不在 F-42d 强做（497 处 rounded-xl
改动面大、收益低，留待后续统一）。
