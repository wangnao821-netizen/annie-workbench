# 端到端联调清单（v2.1.0）

> 前置：`python run_backend.py`（后端 8000）+ 前端 `npm run dev`（3000，VITE_USE_MOCK=false）。
> 每条：操作 → 预期结果。

## 1. 建案
- [ ] 全局咨询 → 快捷提问「服务能力计算器」旁 → 新建案件弹窗（NewCaseSheet）
- [ ] 银行下拉显示 22 家（A 层在前）；选"Commonwealth Bank" → 保存后案件银行显示"CBA"
- [ ] 递交平台下拉显示 MoneyQuest / Infynity / 手动递交
- [ ] 一段话解析：粘贴文字 → 字段预填
- [ ] 上传文件（工资单 PDF）→ 解析预填收入字段
- [ ] 中途关闭 → 重开恢复草稿（localStorage）

## 2. 任务 / SSE
- [ ] 建案后自动产生首批任务（清单/跟进）
- [ ] 开两个浏览器窗口，一边操作（确认清单/记一笔），另一边任务列表实时更新（SSE）

## 3. 草稿与递交
- [ ] 对话生成回复草稿 → 草稿卡出现（披露检查/翻译英文/复制）
- [ ] 翻译/复制可用；未披露内部事实触发黄色警告

## 4. 通知与提醒
- [ ] 通知铃铛有未读提示；点击无遮挡
- [ ] 委派任务设超期 deadline → 调度器（30 分钟）生成 OVERDUE_REMINDER 待办

## 5. 计算器
- [ ] 快捷提问「服务能力计算器」→ 面板：选 CBA → 填样例（base 10万、贷 40万@6%、Single）→ PASS + 步骤轨迹展开
- [ ] 设置页计算器管理：上传 cba xlsm → diff 预览 → 确认应用 → 版本更新 → 回滚可用

## 6. 申报一致性检查
- [ ] 客户全景 → 申报一致性检查卡 → 指定文件/文件夹 → 结论分层（pass/warning/fail）+ 解释信草稿
- [ ] 无外线画像案件 → fail「暂无外线申报画像」

## 7. 能力中心 / 银行平台
- [ ] 设置页能力中心：11 项（6 agent + 5 tool），开关切换 → PATCH 持久（刷新保持）
- [ ] 银行与平台：22 家、A 层在前；勾选平台 + 保存确认 → 徽标变「已确认」；刷新保持

## 8. 统计 / 全景 / 记忆
- [ ] 统计页天/周/月切换，银行维度显示（含 lender_key）
- [ ] 客户全景与 AI 对话上下文同源（context 端点）
- [ ] 案件有 BrainFact 后，摘要/回答体现软记忆（Phase 3）
- [ ] /api/health → config_ok=true；缺 CLIENT_FILES_ROOT 时返回 missing_config

## 9. 备份
- [ ] 手动触发一次备份（调度或脚本）→ core/data/backups/ 生成 assistant_YYYYMMDD_HHMMSS.db
