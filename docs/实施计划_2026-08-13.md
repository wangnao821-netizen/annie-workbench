# 实施计划（2026-08-13 定稿）— Vera Workbench 前后端盘点 + 分阶段路线

> 用途：需要时翻看的总览。数据来源：git 全量提交（截至 4af8654）、工作区状态、前端 vera-工作台 (38) 实码核对、BACKLOG/主文档现役口径。
> 配套：主文档（CASE大脑_产品定位与架构指引.md）、BACKLOG.md、docs/flash_specs/（WO 施工单）。

---

## 一、已完成盘点

### 后端（WO 维度）

| WO | 内容 | 状态 |
|---|---|---|
| WO-01/02/03/07/08 | 核心 44 模块 / API 路由 45 条 / 测试迁移+client_id / 任务引擎+SSE | ✅ |
| WO-13 | 统计时区 Sydney + Alembic URL 加固 + 双库启动自检 | ✅ e3e44e9 关联 |
| WO-14 | 确认闸门（pending→confirmed→superseded） | ✅ |
| WO-15 | BrainFact（fact_schema 42 key + 幂等同步） | ✅ |
| WO-16 | 对话协议（非流式 + 服务端工具循环 + 结构化卡片） | ✅ |
| WO-17 | 上下文注入五层 + 用量测量 + DeepSeek/Gemini 路由 | ✅ |
| WO-18 | 统一建案（LVR/清单预选/存量壳/parse-text/parse-file） | ✅ |
| WO-19 | 政策库规则引擎（policy-check + 建档触发） | ✅ |
| WO-20 | 申报一致性检查 Agent（结论分层 + 解释信 + internal 事件） | ✅ 5c58018 |
| WO-21 | 计算器 Agent（6 家档案机械提取 + 确定性引擎 + 上传闭环） | ✅ edf96f1 |
| WO-22 | 银行主数据 + 聚合平台（registry 22 家/别名/迁移/消费点） | ✅ c25acf0 + d69ca5e |
| WO-23 | PST remember 接线 + pyproject 对齐 + uv.lock 入库 | ✅ b7e27cf + a30a4ef |
| #20 收口 | DB 唯一真源 = core/data/assistant.db（settings/工具/死文件收敛） | ✅ e3e44e9 |
| WO-24 | 记忆层语义检索（sqlite-vec + 本地 BGE + recall 语义层） | ⏳ **已实现未提交**：23 测试过；待 ruff 42 清 + uv.lock 同步 + F821 接线 |
| 专项 | milestone 迁移 / onboarding 补齐 / alembic 单一路径 / 附录 A / 启动自检 | ✅ |

测试基线：704 passed（WO-24 通过后 727）。

### 前端（vera-工作台 (38) 实码核对）

✅ 导航收敛（待办·看板·统计·设置 + 更多折叠）｜✅ 今日工作台首页｜✅ 全局咨询 + 快捷提问（含计算器入口）｜✅ 统一建案 NewCaseSheet（文件解析/一句话/中断恢复）｜✅ 客户全景（context 端点同源）｜✅ 申报一致性检查卡（接真端点）｜✅ 计算器面板 + 设置页计算器管理 + 银行平台只读面板（F-13，3 小修待做）｜✅ 草稿卡披露检查/翻译/复制（#9 主体）｜✅ 能力中心（静态列表）｜✅ 统计页 / 通知 / 旧页面收敛。

---

## 二、未完成盘点

### 后端（按依赖排序）
1. **WO-24 收尾**（在途）：ruff 42 清、uv.lock 重生成、recall.py F821 接线
2. **WO-25 银行×平台 PATCH 端点**（F-13 只读面板的确认闭环）
3. **Agent 注册表端点**（能力中心数据驱动，主文档 §二）
4. **WO-10 调度**（APScheduler：每日备份保留 7 天、委派超期检查、摘要刷新；**剥离文件解析部分**——与"第一版不碰文件"决定对齐）
5. **WO-09 遗留 3 项**：摘要接向量记忆（sqlite-vec 语义，Mem0 不作目标）、CaseChecklist master id 列、generator use_ai=True
6. **WO-12 迁移发布**（版本号三处同步、README/CHANGELOG、release_workflow、可选 CI）
7. **WO-05 Electron**（含系统通知）、**WO-11 微信**、**WO-06 云同步**（后置）
8. 小项：prudent offset 补丁候选、遗留 ruff（import_pst 7 + backfill_client_id 4 + tasks 1）、POST /api/tasks/ 联调临时端点评估

### 前端（按依赖排序）
1. **F-13a 三小修**（mock 名单 6 家真计算器 / tier 按 full-basic 分组 / WO-25 文案）——与后端无关，立即可做
2. **能力中心数据驱动**（等 Agent 注册表端点）
3. **银行×平台确认交互**（等 WO-25）
4. 低频：键盘快捷键（Ctrl+N / Ctrl+/）、设置页 VBA 引导 + API Key、深度工作模式通用版、i18n 预留、a11y 补全
5. 等后端：微信 Bot 前端（WO-11）、Electron 系统通知（WO-05）

> 已划掉的旧 backlog（不再列为差距）：文件预览真实化、邮件/文件详情 AI 分析真实化、知识中心 CRUD 真实化（主文档 #4 知识中心收起、文件/邮件走工具准入三关）、新建案件解析预填真实化（NewCaseSheet 已含）、导航收敛（已做）、缓存命中率测量（WO-17 已交付）。

---

## 三、实施计划（分阶段）

### Phase 0 · 收口在途（本周）
- 后端：WO-24 修复提交（ruff 42 清 + uv.lock + F821）→ 出口 pytest 727 全绿
- 前端：F-13a 三小修（AI Studio 批次）
- 文档：主文档 + 缺口清单（2026-08-13 上午同步）单独 docs commit
- 出口：工作区除 ui 归档外干净

### Phase 1 · 能力中心闭环（前后端并行）
- 后端：WO-25（银行×平台 PATCH）+ Agent 注册表端点（可合成一单）
- 前端：F-14 = 能力中心接注册表 + 银行×平台确认交互
- 出口：设置页能力中心真实可配置、平台确认闭环可写

### Phase 2 · 数据保命（后端）
- WO-10 精简版（每日备份保留 7 天 / 委派超期检查 / 摘要刷新）
- 出口：自动备份 + 超期自动提醒

### Phase 3 · 记忆层深化（后端）
- WO-09 三遗留（摘要接向量记忆 / CaseChecklist master id 列 / generator use_ai=True）
- 出口：摘要带语义软记忆、清单预选质量提升

### Phase 4 · 发布收尾（后端 + 联调）
- WO-12 精简（版本号三处 / README / CHANGELOG / release 流程）+ 端到端联调清单（建案/任务/SSE/草稿/通知）
- 出口：可发布 v2.1 tag

### Phase 5 · 扩展（按需）
- WO-05 Electron（系统通知落地）、WO-11 微信 PoC（个人微信 hook，工具准入三关）、WO-06 云同步（模型稳定后）
- 前端低频项随批次插入

### V2 预留
流式对话（协议已预留）、Screenpipe 被动录入（架构保留）、CopilotKit 生成式 UI、多步流程通用机制（L3）。

---

## 四、当前工作区状态（2026-08-13 晚）

- 未提交：WO-24 文件（5 新 + case_context/recall/db/pyproject 修改）、docs 两份（主文档/缺口清单）、ui/vera-工作台 (33) 删除与 (38) 新增（前端线，按流程不入后端提交）
- 最近提交：4af8654（WO-24 施工单）→ e3e44e9（#20 收口）→ a30a4ef/b7e27cf（WO-23）→ d69ca5e/c25acf0（WO-22）
