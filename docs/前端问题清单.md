# 前端待修改问题清单（2026-08-14 建）

> 工作流：前端由 AI Studio 维护（Codex 不改前端）。Vera 遇到问题 → 记到本清单 → 定稿前集中由 Codex 转成 F 批次提示词 → AI Studio 改 → Vera 导出最新编号到 Downloads → Codex 验收。
> 记录格式：
> - 位置：哪个页面/组件/交互
> - 现象：看到什么（可附图）
> - 期望：想要什么

## 一、待定问题（Vera 收集）
- （Vera 正在收集，待补充）

## 二、已转 F 批次（转出后移到这里）
- **F-19（2026-08-14）**：GapAnalysisCard 字段未对齐后端契约——`missing[].item` 应为 `missing[].name`；`suggestions[].item/suggestion` 应为 `suggestions[].title/description`（WO-33 gap_analysis payload）。当前会显示 undefined。
- （空）
## 三、已直接处理（例外，需 AI Studio 认领同步）
- **设置页顶栏拥挤（2026-08-14）**：Vera 授权直接修于 Downloads (42) `src/pages/Settings.tsx`——顶栏由"标题+5 标签同行挤压"改为**两行**（标题行 + 标签行整行铺开 `gap-2 p-1.5 w-full`）。**AI Studio 下次导出需保留此改动**，否则会回退。