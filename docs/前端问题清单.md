# 前端待修改问题清单（2026-08-14 建）

> 工作流：前端由 AI Studio 维护（Codex 不改前端）。Vera 遇到问题 → 记到本清单 → 定稿前集中由 Codex 转成 F 批次提示词 → AI Studio 改 → Vera 导出最新编号到 Downloads → Codex 验收。
> 记录格式：
> - 位置：哪个页面/组件/交互
> - 现象：看到什么（可附图）
> - 期望：想要什么

## 一、待定问题（Vera 收集）
- （Vera 正在收集，待补充）

## 二、已转 F 批次（转出后移到这里）
- **F-19（2026-08-14，含两问题）**：
  1. GapAnalysisCard 字段未对齐后端契约——`missing[].item` 应为 `missing[].name`；`suggestions[].item/suggestion` 应为 `suggestions[].title/description`（WO-33 gap_analysis payload），当前会显示 undefined
  2. 设置页顶栏拥挤——标题+5 标签同行挤压；期望两行布局 + 标签行间距加大
- **F-40（2026-08-16，(57)，三问题）**：
  1. 文件预览显示解析文本而非原文——期望预览默认显示原文（PDF/图片内嵌），解析内容作第二 tab（依赖 WO-46 raw 端点）
  2. 中栏底部快捷 chips 太多——期望工具类（计算器/邮件等）折叠进「⚡ 工具」按钮放附件旁，chips 行取消
  3. 写补件邮件无反应、无独立悬浮窗——期望 MailComposeModal 弹窗（保存草稿 + 复制英文，无发送按钮）

## 三、已核对结论（2026-08-15，Codex 代码核对）
- **F-38 / (56)：右栏披露标记与风险情报"还是没看到"** → **代码层已实现**：
  - CasePanorama.tsx L354-405：`internal_only` → 🔒 不能给银行看 / `disclosed` → 可披露；hasUndisclosed/undisclosedCount 正确计算；
  - RiskSection.tsx L33-46：风险情报空态「暂无风险提示」+「N 条标记为不能给银行看」警告行；
  - 后端 WO-42：BrainFact.disclosure 列 + PATCH /facts/{id}/disclosure 端点就绪（server/api/cases.py L412）。
  - **用户未看到的最可能原因**：前端跑在 mock 模式（`listBrainFacts` 在 `VITE_USE_MOCK !== 'false'` 时走 mock，mock 数据无 internal_only 事实）；或案件本身无标记事实（空态属正常）。
  - **结论**：无需再改前端代码；连真后端 + 有 internal_only 事实即显示。真机验证时确认 VITE_USE_MOCK=false。
