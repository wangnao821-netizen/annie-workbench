# Annie Workbench 开发与协作最高宪法 (Constitution)

本文档是本工作区所有 AI 协作行为的最高准则（最高优先级，凌驾于所有默认行为之上）。

---

## 🏛️ 第一章：核心协作铁律（五步门禁流程）

在面对用户的任何反馈、修改需求、问题汇报或新功能时，**严禁直接修改代码或发起打包构建**。必须严格遵循以下五步流转：

### 1. 现状求证与事实核查 (Discovery & Root Cause)
- **绝对禁止修改代码**；
- 深入阅读当前源码，定位真实逻辑与现存现象；
- 明确区分：这是真 Bug、配置遗漏，还是设计预期冲突？
- 向用户客观汇报“现状是什么、为什么会发生”。

### 2. 方案推演与多轮沟通 (Brainstorming & Proposals)
- 针对目标，提供 1 ~ 3 个结构化方案；
- 详细说明每个方案的：
  - 核心思路
  - 视觉/交互/架构效果
  - 优劣对比与影响面（前端/后端/打包）
- 与用户展开多轮讨论与细化，推演出最佳路径。

### 3. 方案终审与实施计划 (Sign-off & Plan Approval)
- 整理出最终方案的实施计划清单（涉及文件、组件、风险点）；
- **停下来等待用户明确批准**（如用户回复“确认”、“按此实施”）。
- 未获得明确指令前，禁止进入编码阶段。

### 4. 精确实施与本地验证 (Execution & Verification)
- 严格按照批准的范围修改代码，不做计划外的自作主张；
- 执行本地编译、TypeScript 类型检查与功能测试，确保 0 Error；
- 严禁擅自执行高消耗的打包命令。

### 5. 效果验收与汇报 (Review & Next Steps)
- 汇报修改成果、对比差异与验证结果；
- 等待用户确认体验效果及下一步指令（如确认无误后再讨论是否打包）。

---

## 🛡️ 第二章：与现有 Superpowers Skill 体系的联动

本宪法与已启用的 superpowers 技能体系协同生效：
1. **创意/功能/行为修改** ➔ 严格执行 `superpowers:brainstorming`（含 `<HARD-GATE>` 门禁：未经批准绝不写代码）；
2. **Bug/异常/行为偏离** ➔ 严格执行 `superpowers:systematic-debugging`（含 `Iron Law`：未查清根因绝不提修改）；
3. **复杂多步任务** ➔ 严格执行 `superpowers:writing-plans`。

---

## 🚫 第三章：违规红线 (Forbidden Behaviors)

1. ❌ **抢跑编码**：用户刚描述问题就直接调用文件修改工具；
2. ❌ **擅自打包**：未经用户明确输入“打包”指令，擅自调用 `electron-builder`；
3. ❌ **盲目修补**：在未定位根因的情况下做猜测性代码修补。
